# 安卓应用市场提审工作台 · 产品需求文档（PRD）

| 文档信息 | 内容 |
|---|---|
| 产品名称 | 安卓应用市场提审工作台（appTs） |
| 版本 | v1.0（首版评审通过稿） |
| 日期 | 2026-09-06 |
| 状态 | 已评审通过，进入开发 |
| 定位 | 纯前端个人工具网站（本地优先，无后端） |

修订记录：v0.9 评审稿（2026-09-06）→ v1.0 定稿（2026-09-06，补充多标签同步与数据库细节）。

---

## 1. 背景与目标

国内安卓应用上架的本质是「先合规、后技术」：软著、工信部 APP 备案、ICP 备案、公安联网备案、隐私合规、六市场差异、材料名称一致性等构成一条长而多变的链路，且六家市场（华为 / 小米 / OPPO / vivo / 荣耀 / 应用宝）要求各不相同。本产品将分散在文档、表格与个人经验中的知识固化，为多款 App 的提审提供**一体化工作台**。

**产品目标**
1. 多 App 独立建档，一个工具管理所有产品的提审进度；
2. 按品类自动生成材料清单与自查清单，缩短「该准备什么」的认知成本；
3. 五阶段进度自动派生，始终告诉你「下一步该做什么」；
4. 内置可复制的政策知识库与模板（软著排版、隐私政策、市场字段对照等）；
5. 数据完全本地化、可导入导出，隐私安全。

**验收口径（成功标准）**
- S1 新建一个 App ≤ 2 步，且按品类自动生成材料 + 自查清单；
- S2 新用户按「建议下一步」可在首次会话识别出全部当前阶段待办；
- S3 任何材料 / 自查误操作可回退（状态支持回退、删除需确认）；
- S4 刷新 / 重开不丢数据；导出 JSON 可完整还原；多标签同开实时同步；
- S5 手机浏览器（≥360px，含华为）可完成全部核心操作。

## 2. 术语与政策背景

| 术语 | 说明 |
|---|---|
| 软著 | 《计算机软件著作权登记证书》《APP 电子版权证书》《软件著作权认证证书》三选一；著作权人 = 主体 |
| 工信部 APP 备案 | 2023 年起强制，联网 APP 须取得备案号；在服务器接入商平台办理 |
| ICP 域名备案 | 后端域名 + 国内服务器的工信部网站备案，APP 备案的前置 |
| 公安联网备案 | 联网运行 30 日内在 beian.mps.gov.cn 完成，逾期可警告或停机整顿 |
| 提审 / 复审 | 向应用市场提交审核 / 被驳回修改后再次提交 |
| 测试账号 | 审核员可登录的演示账号，随审核备注提供 |
| 马甲包 | 同产品多副本上架，需独立资质，本产品不引导此类操作 |
| 行业专项资质 | 按品类要求的安全评估报告、网络文化经营许可证、ICP 许可证等 |

## 3. 用户与使用场景

**用户画像**：产品 / 运营负责人本人（非技术向），同时维护多款产品并需陆续上架安卓。

**核心场景**
1. 新建 App 建档（选品类 → 自动生成材料与自查模板）；
2. 每周跟进：材料办理状态、证照编号、测试账号、市场账号进度；
3. 提审前自检：逐项勾选 32 项自查，识别未通过项；
4. 被驳回后：记录驳回原因与整改，市场表单状态回退重提；
5. 对照查询：六市场要求差异、各市场后台字段；
6. 复制模板：隐私政策条款、软著排版说明、应用简介文案。

## 4. 范围

**本版本范围**
- 工作台总览、App 详情（五 Tab）、知识库（9 篇 + 可复制模板）、设置（导入 / 导出 / 重置）。

**范围外（明确不做）**
- 代替向市场提交、对接各市场官方开放 API；
- 云端同步与账号登录；
- 多人协作 / 权限；
- 预置或导入用户个人空间中既有真实 App 数据（无种子数据）；
- iOS App Store 上架管理。

## 5. 功能需求

### 5.1 工作台总览 `/`

| # | 需求点 | 交互与规则 | 验收口径 |
|---|---|---|---|
| F1.1 | 统计条 | 展示：进行中 App 数、建议下一步（取各 App 最早缺口并排序去重后取首条）、平均完成 % | 数据随操作实时刷新 |
| F1.2 | App 卡片网格 | 每卡：名称 + 品类标签 + 五段迷你进度条 + 材料 n/m + 自查 n/32 + 市场 n/6 + 最近更新；点击进详情；空态显示引导与新建按钮 | 多 App 互不串、计数正确 |
| F1.3 | 新建 App | 弹窗两步：①名称 + 包名 + 品类（默认「通用工具」）；②确认。包名同工具内唯一校验（冲突即红字提示）。创建后实例化该品类材料 + 自查快照并跳详情 | 建一个 App ≤2 步 |
| F1.4 | 编辑 / 删除 | 卡片快捷菜单：编辑基本信息；删除需二次确认 + 输入名称前 2 字 | 误删防呆生效 |
| F1.5 | 排序筛选 | 默认按最近更新倒序；支持按阶段/状态筛选（全部 / 资质准备 / 账号注册 / 打包自检 / 提审中 / 已上架） | 筛选不丢数据 |

### 5.2 App 详情 `/app/:id`（左步骤条 + 五 Tab）

**左侧五阶段步骤条**
- 阶段：①资质合规 ②账号注册 ③打包自检 ④提审跟踪 ⑤上架维护。
- 节点态：已完成（绿✓）/ 当前（蓝高亮）/ 未达（灰）。进度由派生规则自动计算（见 §6），不做手动跳级。
- 点击未达阶段 → Popover 列出缺口清单（缺哪些材料 / 自查项 / 市场动作），每项可一键跳对应 Tab。

| # | Tab | 需求点 | 关键规则 |
|---|---|---|---|
| F2.1 | 概览 | 基础信息表单：名称 / 包名（创建后可改但提示影响备案一致性）/ 品类 / 主体名称 / 法人 / 对公账户备注；证照编号汇总卡（软著 / ICP / APP 备案 / 公安备案 / 测试报告）只读回显 | 编号汇总来自材料台账自动回填 |
| F2.2 | 材料 | 分类筛选 chips（全部/主体/知产/备案/包体/应用信息）；分组列表；每行：材料名 + 市场角标（hover 显示哪些市场要求）+ 状态标签 + 编号/URL 输入 + 备注 + 附件文件名列表（可增删，只记名不存内容） | 状态机见 §6；驳回必填原因 |
| F2.3 | 自查 | 五组折叠卡片（资质备案7/包体技术6/隐私合规8/素材文案5/提审信息6=32）；组头进度条；每项通过/不通过 + 不适用开关 + 备注；筛「未通过」模式 | % = 通过 /（总数 − 不适用） |
| F2.4 | 市场 | 六市场 Tabs；每市场：开发者账号状态（未注册/认证中/已认证）、提审状态（未提交/已提交/审核中/已通过/被驳回）、测试账号、最近提交时间、上架时间、版本更新日志列表、驳回原因（驳回时必填）；页尾「市场差异对照」折叠表（只读） | 市场间状态独立 |
| F2.5 | 备注 | 时间线：类型（节点记录/驳回记录/电话核验/资质办理）+ 内容 + 自动时间戳，倒序；可删除 | 操作带时间可回溯 |

### 5.3 知识库 `/knowledge`

| # | 需求点 | 规则 |
|---|---|---|
| F3.1 | 文章阅读 | 左目录（9 篇锚点）+ 右文章；文章懒加载 |
| F3.2 | 搜索 | 按标题/正文关键词过滤，命中高亮 |
| F3.3 | 复制 | 文末「复制全文」；文中每个表格/代码块独立复制按钮 |
| F3.4 | 时效标注 | 每篇页脚徽标：「信息核实于 2026-09 · 政策可能调整，请以各市场官方最新审核要求为准」 |

9 篇文章：①五阶段全程指南 ②软著申请规范与排版 ③APP 备案指引与字段 ④ICP 与公安联网备案 ⑤隐私政策条款模板（含 SDK / 权限清单骨架）⑥六市场差异对照 ⑦高频驳回 TOP6 与对策 ⑧各市场后台字段对照 ⑨常用文案模板。

### 5.4 设置 `/settings`

| # | 需求点 | 规则 |
|---|---|---|
| F4.1 | 导出 | 下载 JSON，文件名 `app-ts-backup-<yyyyMMdd>.json`（含版本号与时间戳） |
| F4.2 | 导入 | FileReader → schema 强校验 → 自动备份当前数据到 `backup-<ts>` → Modal 提示将覆盖 N 条 → 确认写入 |
| F4.3 | 重置 | 需输入「重置」二字按钮才可用；仅清空 appState，保留备份 |
| F4.4 | 数据体检 | 显示 schema 版本、App 条数、条目数、已用存储（navigator.storage.estimate） |
| F4.5 | 隐私说明 | 「数据仅存本机、无任何网络上报、附件仅记文件名」，明示声明 |

## 6. 业务规则（全局）

**R1 材料状态机**
`未开始 → 办理中 → 已下证 / 已上传`（双终态均视为完成）；`已下证 / 已上传 → 被驳回 → 办理中` 可回退；切换为「被驳回」时必填原因；任意状态可填编号 / URL / 备注 / 附件文件名。

**R2 五阶段派生**
- 资质合规 = 类别 ∈ {主体,知产,备案} 且非可选材料中状态 ∈ {已下证,已上传} 的比例（取整 %）；
- 账号注册 = 六市场中「已认证」账号数 / 6；
- 打包自检 = min(包体类材料完成率, 自查「包体技术」组通过率)；
- 提审跟踪 = 六市场中「已通过」数 / 6；上架维护 = 任一市场上架时间非空即完成；
- 当前阶段 = 首个未 100% 的阶段（审核/上架按「已提交任一市场」起算）。
进度为派生计算、不落库，杜绝数据不一致。

**R3 自查公式**
通过率 % = 通过项数 /（总项数 − 不适用项数）；组内同公式；App 完成 % = 各组通过合计 /（32 − 不适用合计）。

**R4 一致性校验**
- 包名在同工具内唯一（新建时校验）；
- 名称 / 包名修改不影响历史数据（保留 updatedAt 供审计）；
- 导入文件先 validateState 强校验，非法直接拒绝并给出原因。

**R5 保存与同步**
- 任一操作 → 派生 updatedAt → 防抖 500ms 写 IndexedDB；
- 顶栏「已保存」弱提示；写失败保留内存副本并提示导出；
- 多标签同开：BroadcastChannel 实时同步（last-write-wins，防回环）。

## 7. 数据字段

实体关系：`PersistState{version, savedAt, apps[]}`；`AppRecord{id, name, packageName, industryId, entity{copyrightName…}, certNo{}, materials[], checklist[], markets[], notes[], createdAt, updatedAt}`。
必填：name / packageName / industryId；其余 optional。完整 TypeScript 定义见《技术方案》§4。字段演进遵循「只加 optional、不改删语义」+ 数据层版本迁移。

## 8. 非功能需求

- 性能：本地毫秒级响应；知识文章懒加载；
- 兼容：桌面 Chrome / Edge；手机浏览器（华为）；视口 ≥360px；离线可用；
- 安全：数据仅存本机、零网络上报；导出由用户自主；导入强校验；
- 可维护：静态知识（代码/内容）与用户数据分离、各自版本化；每篇知识标注核实时间；
- 多标签：BroadcastChannel 实时同步（last-write-wins、防回环）。

## 9. 里程碑口径

M0 文档评审 → M1 骨架 + 数据层 + 总览 → M2 App 详情（步骤 + 材料）→ M3 自查 + 市场 → M4 知识库 + 设置 → M5 打磨交付。逐项验收见《技术方案》§9。

---

## 附录 A · 32 项自查清单（全文）

**资质与备案（7）**
1. 软著证书已下证（三证任选其一）
2. 著作权人 = 开发者账号主体，且 APP 名 = 软著名 = 备案名
3. 转授权链条完整（软著非本主体持有时）
4. 后端服务域名已完成 ICP 备案
5. 工信部 APP 备案号已下发
6. 备案的包名 / 公钥 / 证书 MD5 与上架包体一致
7. 行业专项资质齐全且在有效期内

**包体与技术（6）**
8. 使用 Release 正式签名，非 debug 签名
9. 包体支持 64 位架构
10. targetSdkVersion 满足各市场要求
11. 主流机型无崩溃 / 闪退 / ANR
12. 提审的是最新版，应用内无更新提示
13. 无隐藏图标、捆绑下载、恶意自启动

**隐私合规（8）**
14. 隐私政策为独立可访问网页，非本地文本或图片
15. 首启弹窗明示，含同意与拒绝双选项
16. 同意前 APP 与第三方 SDK 均不采集设备标识符
17. 敏感权限仅在用户使用对应功能时动态申请
18. 拒绝权限后仍可用基础功能、不退出
19. 隐私政策含第三方 SDK 清单与系统权限清单
20. 提供账号注销、撤回同意、删除个人信息入口
21. 注销时限 ≤15 个工作日，且有投诉举报渠道

**素材与文案（5）**
22. 图标 512×512 PNG，与实际安装图标一致
23. 截图 3–5 张，无竞品手机外框与其他商店水印
24. 截图 / 视频内容与实际功能相符
25. 简介无「最 / 第一 / 国家级」等违禁词
26. 应用信息中不含其他终端品牌或应用市场名称

**提审信息（6）**
27. 测试账号可用，备注写明登录步骤
28. 支付 / 实名 / 审核类功能有可走通的演示路径
29. 敏感权限逐条填写场景并附功能截图（OPPO/vivo）
30. 版权证明与备案信息已上传并通过备案校验
31. 分类、内容分级、年龄分级已如实填写
32. 已备客服联系方式与运营主体信息

## 附录 B · 通用基线材料目录（24 项，按品类追加行业项）

| 类别 | 材料 |
|---|---|
| 主体（4） | 营业执照（三证合一）、法人身份证正反面、对公账户信息、经办人授权书（非法人办理时） |
| 知产（2） | 软著证书（三选一，需编号）、软著转授权文件（软著非自有主体时） |
| 备案（5） | 域名 ICP 备案（编号）、工信部 APP 备案（编号）、隐私政策网页上线（URL）、APP 备案承诺书、公安联网备案（编号，上线后 30 日内追踪） |
| 包体（5） | Release 正式签名确认、支持 64 位、targetSdk ≥ 30、主流机型无崩溃闪退、提审最新版 |
| 应用信息（8） | 图标 512×512、截图 3–5 张、简介与版本特性、分类与内容分级、敏感权限说明、测试账号与路径（涉及登录/支付时）、客服与运营主体信息 |

**行业 extra（首期 9 类）**：教育 → 培训前置提示；AI 生成 → 算法备案 + AI 标识 + 安全评估；社交社区 → 安全评估报告；直播娱乐 → 网络文化经营许可证 + 安全评估；电商购物 → ICP 许可证（经营性）；金融理财 / 健康医疗 / 新闻资讯 → 对应牌照与许可。均标注「以官方最新要求为准」。

## 附录 C · 六市场差异速查（主题行 11）
软著要求 / APP 备案 / 隐私检测 / 敏感权限说明 / 64 位 / AI·深度合成标识 / 高敏感类目第三方测试报告 / 截图水印与素材 / 改名限制 / 承诺函与附加证明 / 审核周期与账号类型。

| 市场 | 要点速览 |
|---|---|
| 华为 | 软著建议提供；备案/资质校验最严（证件号校核）；提供隐私检测服务；审核 1–5 工作日 |
| 小米 | 软著强制；截图不得含竞品水印；金融/医疗/新闻类需第三方测试报告；审核 1–3 日 |
| OPPO | 软著电子版权证书；敏感权限逐条填场景+截图；高敏感类目需 CMA/CNAS 报告；审核 1–3 日 |
| vivo | 软著名称须一致；深度合成类需 AI 标识；直播/交友类禁擦边；审核 1–3 日 |
| 荣耀 | 仅接受 64 位应用；改名每年 2 次且需软著；应用名不得为广义归纳词；审核 1–3 日 |
| 应用宝 | 软著可后补；需发布承诺函；平台类需 ICP 许可证；审核 2–7 日 |

> 以上政策信息核实于 2026-09，政策可能调整，请以各市场官方最新要求为准。
