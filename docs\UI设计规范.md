# 安卓应用市场提审工作台 · UI 设计规范

| 文档信息 | 内容 |
|---|---|
| 项目 | appTs（D:\vb_project\appTs） |
| 版本 | v1.0 |
| 日期 | 2026-09-06 |
| 风格 | 现代简洁 · 蓝白 · 卡片式 · 桌面优先 + 响应式 |

---

## 1. 设计令牌（Design Tokens）

> 全部写入 `src/styles/theme.ts` 与 CSS 变量；组件一律引用令牌，禁止散落硬编码色值。

### 1.1 色彩

| 语义 | 令牌 | 浅色值 | 用途 |
|---|---|---|---|
| 品牌主色 | `--brand` | `#2E6BE6` | 主按钮、选中态、链接 |
| 主色浅底 | `--brand-soft` | `#E7F0FD` | 选中行、激活页签、图标底 |
| 主色深 | `--brand-deep` | `#2057C9` | 主色文字、hover |
| 成功/通过 | `--success` | `#1E9E63` | 已下证、已通过、已完成 |
| 警告/进行中 | `--warning` | `#C88719` | 办理中、审核中 |
| 危险/驳回 | `--danger` | `#D94B4B` | 被驳回、删除、重置 |
| 中性·信息 | `--teal` | `#0F6E56` | 已上传（区别于已下证） |
| 正文 | `--text-1` | `#1C2433` | 标题/正文 |
| 次要文字 | `--text-2` | `#5B6675` | 说明、描述 |
| 弱化文字 | `--text-3` | `#96A0AF` | 占位、时间戳、禁用 |
| 分隔线 | `--line` | `#E4E9F1` | 边框、分隔 |
| 页面底色 | `--page-bg` | `#F4F6FA` | 页面/卡片外背景 |
| 卡片底色 | `--card-bg` | `#FFFFFF` | 卡片、导航 |

暗色模式：由 antd `darkAlgorithm` 生成基础色 + kb.css / global.css 提供 `[data-theme='dark']` 变量覆盖（正文→浅色系 #E6EAF2 等，分隔线→#30343D，页面底→#14171C，卡片→#1D2129），保证对比度 ≥ 4.5:1。

### 1.2 语义状态标签（Tag 色板，全站统一）

| 状态 | 浅底 | 文字 | 状态 | 浅底 | 文字 |
|---|---|---|---|---|---|
| 未开始 | `#EEF1F5` | `#5B6675` | 已提交 | `#E7F0FD` | `#2057C9` |
| 办理中 | `#FBF0DC` | `#8A5A0A` | 审核中 | `#FBF0DC` | `#8A5A0A` |
| 已下证 | `#E4F6ED` | `#157347` | 已通过 | `#E4F6ED` | `#157347` |
| 已上传 | `#E1F5EE` | `#0F6E56` | 被驳回 | `#FBEBEB` | `#B23A3A` |

### 1.3 字体 / 间距 / 圆角

- 字体：系统栈（-apple-system, "Segoe UI", "PingFang SC", "Microsoft YaHei"）；正文 14px / 卡片标题 15–16px / 数字强调 24px；字重 400 / 500 / 600（600 仅用于关键数字与主按钮）；
- 间距：4 的倍数（4/8/12/16/20/24/32）；卡片内边距 16–20px，卡片间距 12–16px；
- 圆角：小 6 / 中 8 / 卡片 12；标签与按钮胶囊 `999px`；
- 投影：不使用投影（扁平）；层级靠底色与边框区分。

## 2. antd 组件映射

| 需求 | 组件方案 |
|---|---|
| 顶部导航 | `Layout` + `Menu`（horizontal，选中高亮主色） |
| 统计条 | `Card` + `Statistic` |
| App 卡片 | 自封装 `AppCard`（`Card` + `Tag` + 迷你 `Progress`） |
| 五阶段步骤 | 自封装 `StepSidebar`（仿 `Steps` vertical，custom 节点：完成绿✓ / 当前蓝 / 未达灰） |
| 页签 | `Tabs`（下划线指示器 2px 主色） |
| 材料状态 | `Select` + 状态 `Tag`；驳回原因 `Input` 必填校验 |
| 表单 | `Form`（label 右侧，rules 即时校验） |
| 弹窗 | `Modal`（新建/驳回原因）；删除/导入/重置用 `Modal.confirm` |
| 提示 | `App.useApp()` 的 `message`（「已保存」等弱提示） |
| 市场差异表 | `Table`（size small、首列 sticky、窄屏横向滚动） |
| 折叠 | `Collapse`（自查分组、差异对照） |
| 时间 | `DatePicker`（dayjs，zh-cn locale） |
| 空态 | 自封装 `EmptyState`（图标 + 主文案 + 副文案 + CTA） |
| 复制 | 自封装 `CopyBlock`（clipboard + 成功后 message「已复制」） |

## 3. 页面布局与线框（文字版）

### 3.1 全局框架
```
┌──────────────────────────────────────────────────────┐
│ 顶栏（56px 白底）：Logo「提审工作台」 | 工作台·知识库·设置 │ 已保存态 │
├──────────────────────────────────────────────────────┤
│ 内容区（页面底色 #F4F6FA，最大宽 1200px 居中）          │
│  ↓ 按各页网格/双栏布局                                  │
└──────────────────────────────────────────────────────┘
```

### 3.2 工作台总览 `/`
1. 统计条（3 卡一行，≥992 三列 / <768 单列堆叠）：进行中的 App / 建议下一步 / 平均进度；
2. App 卡片网格：`repeat(auto-fill, minmax(240px, 1fr))`，间距 16px；
   - 卡头：图标方块（品牌浅底 36px）+ 名称（一行省略）+ 品类 Tag + 状态 Tag；
   - 中部：五段迷你进度条（每段圆角 3px，6px 高，完成绿 / 当前黄 / 未达灰）；
   - 卡脚：`材料 n/m · 自查 n/32 · 市场 n/6` + 最近更新；卡片右上角 `…` 快捷菜单（编辑/删除）；
   - 末位卡：虚线「＋ 新建 App」（点击开弹窗）；
3. 空态（无 App）：EmptyState 居中 + 新建主按钮。

### 3.3 新建 App 弹窗
两步合一弹窗（Modal 内步骤提示）：①名称（必填，≤30 字）+ 包名（必填，正则 `[a-z0-9.]`，与已有冲突即时红字）+ 品类（Select 默认「通用工具」，切换时预览将追加的行业材料）+ 主体名称（选填）；②确认摘要（将生成 材料 N 项 + 自查 32 项）→ 创建并跳详情。

### 3.4 App 详情 `/app/:id`
```
┌ 左栏 260px 白卡 ─────────┬────────── 右栏 内容 ──────────┐
│ App 名 / 包名            │ Tabs：材料台账·自查清单·市场提审│
│ ①资质合规  ✓ 15/22      │ ·概览 ·备注（默认落在「材料」） │
│ ②账号注册  ● 2/6  [当前] │                              │
│ ③打包自检  ○ 28/32      │ 内容区（白卡列表 / 分组卡片）    │
│ ④提审跟踪  ○ 0/6        │                              │
│ ⑤上架维护  ○            │                              │
└─────────────────────────┴──────────────────────────────┘
<768px：左栏收起为「当前阶段卡片」，置于右栏顶部；步骤可横向滑动选择。
```
- Tab 概览：Form（只读态编辑）；证照编号汇总为 5 枚小卡只读；
- Tab 材料：分类筛选 chips → 分组标题（类别名 + 完成 n/m）→ 行（材料名 + 角标 Tooltip + 状态 Select + 编号/URL 输入 + 备注 + 附件文件名 + 删除图标）；
- Tab 自查：Collapse 五组；组头「通过 n/m · 进度条」；行 = Checkbox(通过/不通过 三态切换) + 「不适用」开关 + 备注 icon；
- Tab 市场：子 Tabs 六市场 → Form 表单（见 PRD F2.4）+「查看差异对照」折叠 Table；
- Tab 备注：时间线（左侧时间点 + 卡片列表，类型 Tag）。

### 3.5 知识库 `/knowledge`
左目录（Sticky 240px：文章标题锚点，当前高亮）+ 右文章区（白卡 760px）；顶栏下方搜索框（`Input.Search`）。文章内：h1–h4 层级、表格带边框、代码块带底色与「复制」按钮、引用块左竖线、文末核实徽标（Tag）。

### 3.6 设置 `/settings`
分组白卡纵向排列：①数据备份与恢复（导出 / 导入 / 恢复列表：显示最近 5 份备份可一键恢复与删除）②数据重置（输入「重置」激活按钮）③关于（版本、schema、条目、存储用量 Progress、隐私声明）。

## 4. 响应式断点

| 断点 | 行为 |
|---|---|
| ≥1200 | 内容区宽 1200 居中；网格 4 列 |
| 992–1199 | 网格 3 列；详情右栏与左栏保持双栏 |
| 768–991 | 网格 2 列；详情左栏压缩至 220px |
| <768（手机） | 全部单列堆叠；详情左栏转顶部「当前阶段」卡片；卡片点按目标 ≥44px；差异表横向滚动；顶栏导航收起为汉堡（Drawer） |

交互不依赖 hover：关键操作均有触屏可达入口；Tag/Tooltip 信息在移动端以点击展开。

## 5. 文案与可访问性规范

- 全文简体中文；避免「最 / 第一 / 国家级」等极限词（产品文案示例语料见知识库⑨）；
- 数字统一整型百分比显示（如 62%）；编号统一 `材料 n/m · 自查 n/32 · 市场 n/6`；
- 按钮动词化（新建 App / 导出备份 / 确认导入）；危险操作红色文字 + confirm；
- 颜色不作为唯一信息载体：状态同时以 Tag 文字区分；五段进度支持文字计数；
- 表单错误即时 inline 提示（包名重复、驳回原因必填、导入文件非法等）；
- focus 可见（antd 默认 focus ring）；空态、加载骨架（Skeleton）覆盖全部列表页。

## 6. 交付要求

- 页面不出现控制台报错；`npm run build` 通过；
- 360px 视口实测可完成：新建 App、改材料状态、勾选自查、填市场状态、导出；
- 深浅色两套下表格/长文对比度可用；
- 图片/图标无第三方品牌元素；文案为通用占位，不含个人真实项目名。
